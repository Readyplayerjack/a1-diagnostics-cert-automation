# =============================================================================
# Jifeline Networks Partner API Configuration
# =============================================================================
# Required for fetching ticket data, customer information, and conversation text
# Get these credentials from: Jifeline Partner Portal → Settings → API Credentials

# Base URL for the Jifeline Partner API
JIFELINE_API_BASE_URL=https://partner-api-001.prd.jifeline.cloud

# OAuth2 Client ID for Jifeline API authentication
# Found in: Jifeline Partner Portal → Settings → API Credentials
JIFELINE_CLIENT_ID=your_jifeline_client_id_here

# OAuth2 Client Secret for Jifeline API authentication
# Found in: Jifeline Partner Portal → Settings → API Credentials
JIFELINE_CLIENT_SECRET=your_jifeline_client_secret_here

# OAuth2 Token endpoint URL for obtaining access tokens
# TODO: Confirm the exact token URL from Jifeline OAuth documentation
# Typically follows pattern: {BASE_URL}/oauth/token
JIFELINE_TOKEN_URL=https://partner-api-001.prd.jifeline.cloud/oauth/token

# =============================================================================
# Supabase Configuration
# =============================================================================
# Required for certificate storage and retrieval
# Get these from: Supabase Dashboard → Project Settings → API

# Supabase project URL
# Found in: Supabase Dashboard → Project Settings → API → Project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase service role key (for server-side operations)
# Found in: Supabase Dashboard → Project Settings → API → service_role key
# WARNING: Keep this secret secure - it has full database access
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# =============================================================================
# Database Configuration
# =============================================================================
# Required for processed_tickets tracking and direct database queries
# Get this from: Supabase Dashboard → Project Settings → Database → Connection string

# PostgreSQL connection string
# Found in: Supabase Dashboard → Project Settings → Database → Connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://user:password@host:port/database

# =============================================================================
# OpenAI Configuration
# =============================================================================
# Required for vehicle registration and mileage extraction from conversation text
# Used by HttpOpenAiExtractionClient for GPT-4o-mini fallback extraction
# Get this from: https://platform.openai.com/api-keys

# OpenAI API key
# Found in: OpenAI Platform → API Keys → Create new secret key
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI API base URL (optional)
# Defaults to https://api.openai.com/v1 if not set
# Override this if using a proxy or custom endpoint
OPENAI_BASE_URL=https://api.openai.com/v1

# =============================================================================
# PDF Generation Configuration
# =============================================================================
# Development option: Use simple PDF generator (no Chromium required)
# Set to "true" or "1" to use SimpleCertificatePdfGenerator instead of ChromiumCertificatePdfGenerator
# Useful for macOS dev environments where Chromium installation is problematic
# Default: false (uses Chromium for production-quality PDFs)
USE_SIMPLE_PDF=false
